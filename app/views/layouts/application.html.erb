<!DOCTYPE html>
<html>
  <head>
    <title>Employee Recognition Platform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload'%>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light w-100">
        <div class="w-100 container">
          <div class="row w-100 align-items-center justify-content-between">
            <div class="col">
              <h1 >
                <%= link_to 'Logo', employee_root_path, class: 'navbar-brand' %>
              </h1>
            </div>
            <div class="col">
              <ul class="nav justify-content-end ">
                <% if admin_user_signed_in? %>
                  <li><%= link_to "Sign out admin", destroy_admin_user_session_path, method: :delete, class: 'nav-link'  %></li>
                <% elsif !admin_user_signed_in? && !employee_signed_in?  %>
                  <li><%= link_to "Sign in", new_employee_session_path, class: 'nav-link'  %></li>
                  <li><%= link_to "Sign up", new_employee_registration_path, class: 'nav-link'  %></li>
                <% else %>
                  <li><%= link_to "Sign out", destroy_employee_session_path, method: :delete, class: 'nav-link'  %></li>
                <% end %>
              </ul>
            </div> 
          </div>
        </div>
      </nav>
    </header>
    <div class="flashMsg">
      <div class="container">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>
    <div class="container">
    <%= yield %>
    </div>
  </body>
</html>
